#!/bin/bash

set -e

DOTFILES_DIR="$HOME/.dotfiles"

if ! [[ -d "$DOTFILES_DIR" ]]; then
    git clone https://github.com/julian-groen/dotfiles.git $DOTFILES_DIR
else
    git -C $DOTFILES_DIR pull
fi

ansible-galaxy install -r $DOTFILES_DIR/requirements/*